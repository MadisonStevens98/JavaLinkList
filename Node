/*
Madison Stevens
CompSci 182/182L
11-14-17
 */
//this .java defines what a node object is and what it contains
package project5;

class Node { 
    int iData;           // this is the key Is it tree overhead or real data ? 
    double dData;        // extra data to store in Node, iData (the key) more important 
    Node leftChild;     // tree overhead, not 'real' information 
    Node rightChild;   // tree overhead, not 'real' information 
}
